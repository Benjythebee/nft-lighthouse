

// just for the type
export const middleware = (req:Request):Response => new Response()

//just for the type
export const pathMiddleware = (...middlewareFunctions:(typeof middleware)[])=>{

    return new Response()
}